<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | WEJÀ Target
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Fira+Code:wght@400;500&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <header class="header">
        <h1>🎯 Target Server</h1>
        <nav class="header-nav">
            <a href="/">Dashboard</a>
            <a href="/search">Search</a>
            <a href="/login">Login</a>
            <a href="/comment" class="active">Comments</a>
            <a href="/file">Files</a>
        </nav>
        <div class="db-status <%= dbStatus %>">
            <span class="status-dot"></span>
            MongoDB: <%= dbStatus %>
        </div>
    </header>

    <main class="container">
        <div class="page-title">
            <h2>💬 Comments</h2>
            <p>Test XSS detection with comment submissions</p>
        </div>

        <div class="grid grid-2">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Post a Comment</h3>
                </div>
                <div class="card-body">
                    <% if (message) { %>
                        <div class="alert alert-success">
                            ✅ <%= message.text %>
                        </div>
                        <% } %>

                            <form action="/comment" method="POST">
                                <div class="form-group">
                                    <label for="author">Your Name (optional)</label>
                                    <input type="text" id="author" name="author" class="form-control"
                                        placeholder="Anonymous">
                                </div>
                                <div class="form-group">
                                    <label for="content">Comment</label>
                                    <textarea id="content" name="content" class="form-control"
                                        placeholder="Write your comment..." required></textarea>
                                </div>
                                <button type="submit" class="btn btn-primary">💬 Post Comment</button>
                            </form>

                            <div class="hint-box" style="margin-top: 1.5rem;">
                                <h4>💡 Test XSS Attacks</h4>
                                <ul>
                                    <li><code>&lt;script&gt;alert('XSS')&lt;/script&gt;</code></li>
                                    <li><code>&lt;img src=x onerror=alert('XSS')&gt;</code></li>
                                    <li><code>&lt;svg onload=alert('XSS')&gt;</code></li>
                                    <li><code>javascript:alert('XSS')</code></li>
                                </ul>
                            </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Recent Comments</h3>
                    <span style="color: #6c6c80; font-size: 0.85rem;">
                        <%= comments.length %> comments
                    </span>
                </div>
                <div class="card-body" style="max-height: 500px; overflow-y: auto;">
                    <% if (comments.length> 0) { %>
                        <% comments.forEach(function(comment) { %>
                            <div class="comment">
                                <div class="comment-author">
                                    <%= comment.author %>
                                </div>
                                <div class="comment-content">
                                    <%= comment.content %>
                                </div>
                                <div class="comment-time">
                                    <%= new Date(comment.createdAt).toLocaleString() %>
                                </div>
                            </div>
                            <% }); %>
                                <% } else { %>
                                    <div class="empty-state">
                                        <div class="empty-state-icon">💬</div>
                                        <p>No comments yet. Be the first!</p>
                                    </div>
                                    <% } %>
                </div>
            </div>
        </div>

        <div class="proxy-info">
            <p>💡 Access via WAF proxy:</p>
            <code>POST http://localhost:3000/proxy/comment</code>
        </div>
    </main>
</body>

</html>