<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | WEJÀ Target</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header class="header">
        <h1>🎯 Target Server</h1>
        <nav class="header-nav">
            <a href="/" class="<%= title === 'Target Dashboard' ? 'active' : '' %>">Dashboard</a>
            <a href="/search" class="<%= title === 'Search' ? 'active' : '' %>">Search</a>
            <a href="/login" class="<%= title === 'Login' ? 'active' : '' %>">Login</a>
            <a href="/comment" class="<%= title === 'Comments' ? 'active' : '' %>">Comments</a>
            <a href="/file" class="<%= title === 'File Viewer' ? 'active' : '' %>">Files</a>
        </nav>
        <div class="db-status <%= dbStatus %>">
            <span class="status-dot"></span>
            MongoDB: <%= dbStatus %>
        </div>
    </header>
    
    <main class="container">
        <%- body %>
    </main>
</body>
</html>
